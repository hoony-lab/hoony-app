language: java
jdk:
  - openjdk8

branches:
  only:
    - main

# Travis CI 서버 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_deploy:
  - zip -r hoony-app *
  - mkdir -p deploy
  - mv hoony-app.zip deploy/hoony-app.zip

deploy:
  - provider: s3
    access_key_id: $AWS_ACESS_KEY
    secret_access_key: $AWS_SECRET_KEY
    bucket: hoony-app-build
    region: ap-northeast-2
    skip_cleanup: true
    acl: private
    local_dir: deploy
    wait-until-deployed: true
  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_SECRET_KEY
    bucket: hoony-app-build
    key: hoony-app.zip
    bundle_type: zip
    application: hoony-app
    deployment_group: hoony-app-group
    regin: ap-northeast-2
    wait-until-deployed: true

# CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - jihun9838@gmail.com